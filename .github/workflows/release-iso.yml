name: Release Abora ISO

permissions:
  contents: write

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

on:
  push:
    tags:
      - "v*"

jobs:
  release-iso:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Read version
        id: version
        run: |
          set -euo pipefail
          value="$(tr -d '\n' < VERSION | tr -cd '[:alnum:]._-')"
          case "$value" in
            [Vv]*) tag="$value" ;;
            *) tag="v$value" ;;
          esac

          if [ "$tag" != "$GITHUB_REF_NAME" ]; then
            echo "VERSION ($tag) does not match pushed tag ($GITHUB_REF_NAME)." >&2
            exit 1
          fi

          echo "value=$value" >> "$GITHUB_OUTPUT"
          echo "tag=$tag" >> "$GITHUB_OUTPUT"

      - name: Install Nix (official installer, no FlakeHub)
        run: |
          set -euxo pipefail
          curl -fsSL https://nixos.org/nix/install -o /tmp/install-nix.sh
          sh /tmp/install-nix.sh --no-daemon
          echo "$HOME/.nix-profile/bin" >> "$GITHUB_PATH"
          "$HOME/.nix-profile/bin/nix" --version

      - name: Build Release Bundle
        id: build_iso
        continue-on-error: true
        env:
          NIX_CONFIG: |
            experimental-features = nix-command flakes
            access-tokens = github.com=${{ github.token }}
        run: |
          set -euxo pipefail
          export PATH="$HOME/.nix-profile/bin:$PATH"
          nix --version
          make release 2>&1 | tee /tmp/abora-build-iso.log

      - name: Show Build Log Tail
        if: always()
        run: |
          echo "=== build log tail ==="
          tail -n 200 /tmp/abora-build-iso.log || true

      - name: Show output files
        if: steps.build_iso.outcome == 'success'
        run: find out -maxdepth 2 -type f -print | sort

      - name: Upload Build Log Artifact
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: abora-release-log-${{ github.run_number }}
          path: /tmp/abora-build-iso.log
          if-no-files-found: warn

      - name: Upload Release Bundle
        if: steps.build_iso.outcome == 'success'
        uses: actions/upload-artifact@v7
        with:
          name: abora-${{ steps.version.outputs.tag }}-release-${{ github.run_number }}
          # The ISO itself is not attached here — the ISO workflow/hosting
          # step publishes it separately. This keeps the ISO out of both the
          # workflow artifact and the GitHub Release assets below.
          path: |
            out/packages/tinypm-*-abora-${{ steps.version.outputs.tag }}.tar.gz
            out/packages/anix-*-abora-${{ steps.version.outputs.tag }}.tar.gz
            out/release/SHA256SUMS-${{ steps.version.outputs.tag }}.txt
            out/release/RELEASE_MANIFEST-${{ steps.version.outputs.tag }}.txt
            out/release/RELEASE_NOTES-${{ steps.version.outputs.tag }}.md
          if-no-files-found: error

      - name: Create Draft GitHub Release
        if: steps.build_iso.outcome == 'success'
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: |
          set -euxo pipefail
          tag="${{ steps.version.outputs.tag }}"
          title="Abora OS v4 Everest (${tag})"
          notes_file="out/release/RELEASE_NOTES-${tag}.md"
          shopt -s nullglob
          # ISO is intentionally not attached to the GitHub Release — it is
          # published separately by the ISO workflow/hosting step. Only the
          # smaller release artifacts (TinyPM package, checksums, manifest)
          # go here.
          assets=(
            out/packages/tinypm-*-abora-"$tag".tar.gz
            out/packages/anix-*-abora-"$tag".tar.gz
            out/release/SHA256SUMS-"$tag".txt
            out/release/RELEASE_MANIFEST-"$tag".txt
          )

          if gh release view "$tag" >/dev/null 2>&1; then
            gh release edit "$tag" --title "$title" --notes-file "$notes_file" --draft
            gh release upload "$tag" "${assets[@]}" --clobber
          else
            gh release create "$tag" "${assets[@]}" --title "$title" --notes-file "$notes_file" --draft
          fi

      - name: Fail Release If ISO Step Failed
        if: steps.build_iso.outcome != 'success'
        run: |
          echo "ISO build failed; see abora-release-log artifact."
          if [ -f /tmp/abora-build-iso.log ]; then
            tail_txt="$(tail -n 40 /tmp/abora-build-iso.log)"
            esc_tail="$(printf '%s' "$tail_txt" | sed -e 's/%/%25/g' -e 's/\r/%0D/g' -e ':a;N;$!ba;s/\n/%0A/g')"
            echo "::error title=ISO build failure tail::$esc_tail"
          else
            echo "::error title=ISO build failure::No /tmp/abora-build-iso.log file found."
          fi
          exit 1
